import 'package:flutter/material.dart';

class ProposalsInvoicingScreen extends StatelessWidget {
  const ProposalsInvoicingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Proposals & Invoices')),
      body: const Center(child: Text('Proposals & invoicing — placeholder')),
    );
  }
}
